<h2 class="supertitle">First Church Deacons</h2>
<div class="header-row">
  <h3 class="header-title">{{@title}}</h3>
  {{#if @icon}}
    <button 
      class="header-right-icon" 
      {{action @iconclick on="tap"}} 
    >
      {{fa-icon @icon}}
    </button>
  {{/if}}
</div>
