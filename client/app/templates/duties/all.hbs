<RoleList 
  class="page-frame" 
  @sourceData={{this.model.occasions}}
  @showNames={{true}} 
  @clickType="confirm" 
  @holdType="decline" 
  @permit={{action this.permit}}
  @action={{action this.change}}
/>